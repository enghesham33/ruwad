import 'package:flutter/material.dart';

class ExamSummaryType {
  final String name;
  final double percent;
  final Color color;

  ExamSummaryType({
    required this.name,
    required this.percent,
    required this.color,
  });
}